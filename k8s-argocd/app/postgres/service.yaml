apiVersion: v1
kind: Service
metadata:
  name: bmi-postgres-svc
  namespace: bmi-app
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - port:       5432
      targetPort: 5432
      protocol:   TCP
