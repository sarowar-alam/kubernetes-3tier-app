apiVersion: v1
kind: Service
metadata:
  name: bmi-postgres-svc
  namespace: bmi-app
spec:
  # ClusterIP so only in-cluster pods can reach the DB
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - port:       5432
      targetPort: 5432
      protocol:   TCP
